PORT=5000
NODE_ENV=development
JWT_SECRET=super_secure_fake_info_detector_jwt_secret_key_2026
JWT_EXPIRES_IN=7d

# Database Configuration (Defaults to local SQLite db, can be switched to PostgreSQL)
DATABASE_URL=sqlite://./fakeinfo.sqlite

# AI Engine FastAPI URL
AI_ENGINE_URL=http://localhost:8000

# File Uploads Directory
UPLOAD_DIR=./uploads
